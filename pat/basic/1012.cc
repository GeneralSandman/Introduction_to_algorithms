#include <iostream>
#include <vector>
#include <string>
#include <iterator>
#include <algorithm>

using namespace std;

vector<int> tmp;
vector<vector<int>> res(5, tmp);

int fun1(int n) {}  

int main()
{
    int n = 0;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        int tmp = 0;
        cin >> tmp;
    }
    return 0;
}
